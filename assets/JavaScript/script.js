// link buttons in header to their sections;
// link resume
// link instagram, github, linkedin
// click on images and have them expand
    // set display properties to "none" in css?
    // create a show() function in JS, which can access the image and change the display property to block
    // add this to html code?